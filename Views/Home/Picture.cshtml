@model PictureThing.Models.UploadedImage
<link rel="stylesheet" href="~/Content/pictureStyle.css" />
@for(int i = 0; i < Model.Colors.Count();i++){for(int j = 0; j < Model.Colors[i].Count(); j++){<span class="@Model.CssClassAssignments[i][j]">@Model.Chars[i][j]</span>}<br />}
